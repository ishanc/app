[1mdiff --git a/file_server/app.py b/file_server/app.py[m
[1mindex e7b63cc..a952f2a 100644[m
[1m--- a/file_server/app.py[m
[1m+++ b/file_server/app.py[m
[36m@@ -1,6 +1,11 @@[m
 from flask import Flask, render_template, request, send_from_directory, jsonify[m
 import os[m
 import sys[m
[32m+[m[32msys.path.append(os.path.abspath(os.path.join(os.path.dirname(__file__), '..')))[m
[32m+[m[32msys.path.append(os.path.abspath(os.path.join(os.path.dirname(__file__), '..', 'lasVegas', 'app')))[m
[32m+[m[32mfrom lms_error_analyzer.database.db_utils import DatabaseConnectionManager[m
[32m+[m[32m#sys.path.append(os.path.abspath(os.path.join(os.path.dirname(__file__), '..', '..')))[m
[32m+[m[32m#import sys[m
 from werkzeug.utils import secure_filename[m
 import time[m
 import pandas as pd[m
[36m@@ -8,12 +13,26 @@[m [mfrom datetime import datetime[m
 import logging[m
 import debugpy[m
 from typing import List, Optional[m
[32m+[m[32mfrom lms_error_analyzer.database.pdf_quality_report import get_total_records_processed[m
[32m+[m[32m#from pdf_quality_report import get_total_records_processed[m
 [m
 # Configure logging[m
 logging.basicConfig(level=logging.WARNING)[m
 logging.getLogger('mysql.connector').setLevel(logging.WARNING)[m
 logger = logging.getLogger(__name__)[m
 [m
[32m+[m[32m# [MODIFIED: 2025-09-17] Previous path management approach was less organized[m
[32m+[m[32m"""[m
[32m+[m[32mOriginal implementation:[m
[32m+[m[32mimport sys[m
[32m+[m[32mimport os[m
[32m+[m[32msys.path.append(os.path.abspath(os.path.join(os.path.dirname(__file__), '..')))[m
[32m+[m[32msys.path.append(os.path.abspath(os.path.join(os.path.dirname(__file__), '..', 'lasVegas', 'app')))[m
[32m+[m[32msys.path.append(os.path.abspath(os.path.join(os.path.dirname(__file__), '..', 'lms_error_analyzer')))[m
[32m+[m
[32m+[m[32mReason for change: Improved organization and clarity of path management[m
[32m+[m[32m"""[m
[32m+[m
 # Add the lasVegas app directory to Python path[m
 LASVEGAS_APP_DIR = os.path.abspath(os.path.join(os.path.dirname(__file__), '..', 'lasVegas', 'app'))[m
 sys.path.append(LASVEGAS_APP_DIR)[m
[36m@@ -22,11 +41,7 @@[m [msys.path.append(LASVEGAS_APP_DIR)[m
 LMS_DATABASE_DIR = os.path.abspath(os.path.join(os.path.dirname(__file__), '..', 'lms_error_analyzer', 'database'))[m
 sys.path.append(LMS_DATABASE_DIR)[m
 [m
[31m-import sumtotal_transformer_with_neo4j as transformer[m
[31m-[m
[31m-# Import session manager[m
[31m-sys.path.append(LMS_DATABASE_DIR)[m
[31m-from session_manager import session_manager[m
[32m+[m[32mfrom lasVegas.app import sumtotal_transformer_with_neo4j as transformer[m
 [m
 app = Flask(__name__)[m
 [m
[36m@@ -53,39 +68,65 @@[m [mdef allowed_file(filename):[m
 def index():[m
     return render_template('index.html')[m
 [m
[31m-@app.route('/lms')[m
[31m-def lms_ui():[m
[31m-    """Serve the new LMS Migration Agent UI"""[m
[31m-    return send_from_directory('.', 'index.html')[m
[31m-[m
 def map_filename_to_database_key(filename):[m
     """Map filename to the correct database key for mapping rules"""[m
[31m-    # Import here to avoid circular imports[m
[31m-    sys.path.append(os.path.join(os.path.dirname(__file__), '..', 'lms_error_analyzer', 'database'))[m
[31m-    from utils.filename_mapper import FilenameMapper[m
[32m+[m[32m    # Implement direct mapping for Core files to maintain consistent keys[m
[32m+[m[32m    if filename.startswith('Core_'):[m
[32m+[m[32m        base_name = os.path.splitext(filename)[0][m
[32m+[m[32m        return base_name  # Keep the full Core_ prefix for consistency[m
[32m+[m[41m        [m
[32m+[m[32m    # For other files, use the FilenameMapper[m
[32m+[m[32m    from lms_error_analyzer.database.utils.filename_mapper import FilenameMapper[m
     return FilenameMapper.to_db_key(filename)[m
 [m
[32m+[m[32m# [MODIFIED: 2025-09-17] Previous validation was simpler without detailed checks[m
[32m+[m[32m"""[m
[32m+[m[32mOriginal implementation:[m
[32m+[m[32mdef validate_transformed_data(df, mapping_rules):[m
[32m+[m[32m    errors = [][m
[32m+[m[32m    for rule in mapping_rules:[m
[32m+[m[32m        if rule.get('mandatory') == 'Mandatory' and rule['CSOD Field Name'] not in df.columns:[m
[32m+[m[32m            errors.append(f"Mandatory field '{rule['CSOD Field Name']}' is missing")[m
[32m+[m[32m    return {[m
[32m+[m[32m        'total_errors': len(errors),[m
[32m+[m[32m        'errors': errors[m
[32m+[m[32m    }[m
[32m+[m
[32m+[m[32mReason for change: Added more comprehensive validation including empty values and warnings tracking[m
[32m+[m[32m"""[m
[32m+[m
 def validate_transformed_data(df, mapping_rules):[m
     """Validate the transformed data against mapping rules"""[m
     errors = [][m
     warnings = []  # Keep for internal logging only[m
     [m
[32m+[m[32m    # Build field mapping dictionary[m
[32m+[m[32m    field_mapping = {}[m
[32m+[m[32m    reverse_mapping = {}[m
[32m+[m[32m    for rule in mapping_rules:[m
[32m+[m[32m        csod_field = rule.get('CSOD Field Name', '')[m
[32m+[m[32m        st_field = rule.get('SumTotal Field Name', '')[m
[32m+[m[32m        if st_field and csod_field:[m
[32m+[m[32m            field_mapping[st_field] = csod_field[m
[32m+[m[32m            reverse_mapping[csod_field] = st_field[m
[32m+[m[41m    [m
     for rule in mapping_rules:[m
         csod_field = rule['CSOD Field Name'][m
[32m+[m[32m        st_field = reverse_mapping.get(csod_field)[m
         mandatory = rule.get('mandatory', '') == 'Mandatory'[m
         [m
[31m-        if csod_field in df.columns:[m
[31m-            # Check for mandatory fields that are empty[m
[32m+[m[32m        if st_field and st_field in df.columns:[m
[32m+[m[32m            # Check mandatory fields using source field values[m
             if mandatory:[m
[31m-                empty_count = (df[csod_field].isna() | (df[csod_field].astype(str).str.strip() == '')).sum()[m
[32m+[m[32m                empty_count = (df[st_field].isna() | (df[st_field].astype(str).str.strip() == '')).sum()[m
                 if empty_count > 0:[m
[31m-                    warnings.append(f"Mandatory field '{csod_field}' has {empty_count} empty values")[m
[32m+[m[32m                    warnings.append(f"Mandatory field '{st_field}' mapped to '{csod_field}' has {empty_count} empty values")[m
         else:[m
[31m-            # Field is missing from output[m
[32m+[m[32m            # Field is missing from input[m
             if mandatory:[m
[31m-                errors.append(f"Mandatory field '{csod_field}' is missing from output")[m
[32m+[m[32m                errors.append(f"Mandatory field '{st_field}' mapped to '{csod_field}' is missing from input")[m
             else:[m
[31m-                warnings.append(f"Optional field '{csod_field}' is missing from output")[m
[32m+[m[32m                warnings.append(f"Optional field '{st_field}' mapped to '{csod_field}' is missing from input")[m
     [m
     # Log warnings internally but don't return them to UI[m
     if warnings:[m
[36m@@ -98,8 +139,22 @@[m [mdef validate_transformed_data(df, mapping_rules):[m
         'warnings': []  # Always return empty array for UI[m
     }[m
 [m
[32m+[m[32m# [MODIFIED: 2025-09-17] Added improved file processing with validation and error checks[m
[32m+[m[32m"""[m
[32m+[m[32mOriginal implementation:[m
 def process_file(filepath):[m
[31m-    """Process a file using the SumTotal transformer"""[m
[32m+[m[32m    filename = os.path.basename(filepath)[m
[32m+[m[32m    file_key = map_filename_to_database_key(filename)[m
[32m+[m[32m    mapping_rules = transformer.fetch_mapping_rules_from_neo4j(file_key)[m
[32m+[m[32m    input_df = pd.read_excel(filepath)[m
[32m+[m[32m    transformed_df = transformer.transform_sumtotal_file(input_df, mapping_rules, file_key)[m
[32m+[m[32m    return transformed_df[m
[32m+[m
[32m+[m[32mReason for change: Added comprehensive error checking, validation, and improved file handling[m
[32m+[m[32m"""[m
[32m+[m
[32m+[m[32mdef process_file(filepath):[m
[32m+[m[32m    """Process a file using the SumTotal transformer with enhanced validation"""[m
     try:[m
         # Fetch mapping rules directly from Neo4j database[m
         filename = os.path.basename(filepath)[m
[36m@@ -142,12 +197,25 @@[m [mdef process_file(filepath):[m
         transformed_df = transformer.transform_sumtotal_file(input_df, mapping_rules, file_key, filename) #Pass original filename[m
         try:[m
             sys.path.append(os.path.join(os.path.dirname(__file__), '..', 'lms_error_analyzer', 'database'))[m
[31m-            from dashboard import Dashboard[m
[31m-            from report_generator import generate_anomaly_report_csv[m
[32m+[m[32m            from lms_error_analyzer.database.dashboard import Dashboard[m
[32m+[m[32m            from lms_error_analyzer.database.report_generator import generate_anomaly_report_csv[m
[32m+[m[41m            [m
[32m+[m[32m            # [MODIFIED: 2025-09-16] Changed Dashboard initialization to use connection manager[m
[32m+[m[32m            """[m
[32m+[m[32m            Original implementation:[m
             dashboard = Dashboard()[m
             completeness_metrics = dashboard.calculate_file_completeness(filename, input_df)[m
             dashboard.store_completeness_metrics(filename, completeness_metrics)[m
[31m-            dashboard.close_connections()[m
[32m+[m[32m            dashboard.close_connection()[m
[32m+[m
[32m+[m[32m            Reason for change: Improved connection management using context manager to ensure proper cleanup[m
[32m+[m[32m            """[m
[32m+[m[32m            with DatabaseConnectionManager() as db_conn:[m
[32m+[m[32m                dashboard = Dashboard()  # Dashboard creates its own connection[m
[32m+[m[32m                completeness_metrics = dashboard.calculate_file_completeness(filename, input_df)[m
[32m+[m[32m                dashboard.store_completeness_metrics(filename, completeness_metrics)[m
[32m+[m[32m                # Connection will be automatically closed by context manager[m
[32m+[m[41m                [m
             # Generate MVP anomaly CSV report alongside processed files[m
             try:[m
                 report_filename = generate_anomaly_report_csv(filename, app.config['PROCESSED_FOLDER'])[m
[36m@@ -234,18 +302,6 @@[m [mdef upload_file():[m
             [m
             logger.info(f"Processing file: {filename}")[m
             [m
[31m-            # Initialize session if needed - ensure we have active session for file tracking[m
[31m-            if not session_manager.current_session:[m
[31m-                try:[m
[31m-                    session_id = session_manager.start_new_session("web_user")[m
[31m-                    logger.info(f"✅ Started new upload session: {session_id}")[m
[31m-                except Exception as session_init_error:[m
[31m-                    logger.error(f"❌ Failed to initialize session: {session_init_error}")[m
[31m-                    # Continue without session tracking for now[m
[31m-                    pass[m
[31m-            else:[m
[31m-                logger.info(f"📋 Using existing session: {session_manager.current_session.session_id}")[m
[31m-            [m
             # Process the file using the transformer[m
             try:[m
                 processed_results = process_file(filepath)[m
[36m@@ -256,68 +312,26 @@[m [mdef upload_file():[m
                     os.remove(filepath)[m
                 return jsonify({'error': f'Processing error: {str(process_error)}'}), 500[m
             [m
[31m-            # Track file in session after successful processing - critical for orphan detection scope[m
[31m-            if processed_results.get('success'):[m
[31m-                try:[m
[31m-                    sys.path.append(LMS_DATABASE_DIR)[m
[31m-                    from utils.filename_mapper import FilenameMapper[m
[31m-                    [m
[31m-                    # Map filename to frontend display name and get row count[m
[31m-                    frontend_name = FilenameMapper.to_frontend_name(filename)[m
[31m-                    logger.info(f"📁 Mapping {filename} -> {frontend_name}")[m
[31m-                    [m
[31m-                    # Extract row count from processing results[m
[31m-                    row_count = 0[m
[31m-                    if 'validation' in processed_results and 'total_records' in processed_results['validation']:[m
[31m-                        row_count = processed_results['validation']['total_records'][m
[31m-                    [m
[31m-                    # Generate simple checksum for file integrity tracking[m
[31m-                    import hashlib[m
[31m-                    checksum = hashlib.md5(filename.encode()).hexdigest()[:8][m
[31m-                    [m
[31m-                    logger.info(f"📊 Session tracking: {frontend_name}, rows: {row_count}, checksum: {checksum}")[m
[31m-                    [m
[31m-                    # Record file upload in session for domain-scoped orphan detection[m
[31m-                    if session_manager.current_session:[m
[31m-                        logger.info(f"📋 Recording upload in session: {session_manager.current_session.session_id}")[m
[31m-                        [m
[31m-                        success = session_manager.add_uploaded_file([m
[31m-                            front_end_name=frontend_name,[m
[31m-                            original_file_name=filename,[m
[31m-                            row_count=row_count,[m
[31m-                            checksum_md5=checksum,[m
[31m-                            uploader="web_user"[m
[31m-                        )[m
[31m-                        [m
[31m-                        if success:[m
[31m-                            logger.info(f"✅ Successfully tracked in session: {frontend_name} ({row_count} rows)")[m
[31m-                        else:[m
[31m-                            logger.error(f"❌ Failed to track {frontend_name} in session")[m
[31m-                    else:[m
[31m-                        logger.warning("⚠️ No active session available for file tracking - orphan detection may include historical data")[m
[31m-                    [m
[31m-                except Exception as session_error:[m
[31m-                    logger.error(f"❌ Error tracking file in session: {session_error}")[m
[31m-                    # Don't fail the upload if session tracking fails - allow processing to continue[m
[31m-            [m
             # Clean up the original file[m
             try:[m
                 os.remove(filepath)[m
             except Exception as cleanup_error:[m
                 logger.warning(f"Could not clean up original file: {cleanup_error}")[m
             [m
[31m-            # Auto-generate PDF report after successful processing using original filenames[m
[32m+[m[32m            # Auto-generate consolidated PDF report after successful processing[m
             try:[m
[31m-                # Ensure proper import path for PDF generation[m
[31m-                sys.path.append(LMS_DATABASE_DIR)[m
[31m-                from pdf_quality_report import auto_generate_after_upload[m
[31m-                # auto_generate_after_upload now gets original files from database automatically[m
[32m+[m[32m                sys.path.append(os.path.join(os.path.dirname(__file__), '..', 'lms_error_analyzer', 'database'))[m
[32m+[m[32m                from lms_error_analyzer.database.pdf_quality_report import auto_generate_after_upload, cleanup_old_pdf_reports[m
[32m+[m[32m                # First cleanup any existing PDFs[m
[32m+[m[32m                cleanup_old_pdf_reports(app.config['PROCESSED_FOLDER'])[m
[32m+[m[32m                # Generate new consolidated report[m
                 pdf_filename = auto_generate_after_upload([], app.config['PROCESSED_FOLDER'])  # Empty list, function gets files from DB[m
                 if pdf_filename:[m
                     processed_results['pdf_report'] = pdf_filename[m
[31m-                    logger.info(f"Auto-generated PDF report: {pdf_filename}")[m
[32m+[m[32m                    logger.info(f"Auto-generated consolidated PDF report: {pdf_filename}")[m
             except Exception as pdf_error:[m
                 logger.error(f"Error auto-generating PDF report: {pdf_error}")[m
[32m+[m[32m                logger.error(f"PDF error details: {str(pdf_error)}")[m
                 # Don't fail the upload if PDF generation fails[m
             [m
             return jsonify(processed_results)[m
[36m@@ -381,37 +395,34 @@[m [mdef delete_file(filename):[m
         logger.error(f"Error deleting file {filename}: {e}")[m
         return jsonify({'error': str(e)}), 500[m
 [m
[32m+[m[32m# [MODIFIED: 2025-09-17] Enhanced reset functionality with better error handling[m
[32m+[m[32m"""[m
[32m+[m[32mOriginal implementation:[m
 @app.route('/reset-all-data', methods=['POST'])[m
 def reset_all_data_endpoint():[m
[31m-    """Reset all data for a new batch - clears database, files, and session data"""[m
[32m+[m[32m    try:[m
[32m+[m[32m        reset_all_data(app.config['PROCESSED_FOLDER'])[m
[32m+[m[32m        return jsonify({'success': True})[m
[32m+[m[32m    except Exception as e:[m
[32m+[m[32m        return jsonify({'error': str(e)}), 500[m
[32m+[m
[32m+[m[32mReason for change: Added comprehensive reset with better error tracking and detailed response[m
[32m+[m[32m"""[m
[32m+[m
[32m+[m[32m@app.route('/reset-all-data', methods=['POST'])[m
[32m+[m[32mdef reset_all_data_endpoint():[m
[32m+[m[32m    """Reset all data for a new batch - clears database and all processed files"""[m
     logger.info("🔍 Reset all data endpoint called") # DEBUG[m
     try:[m
         # Import from the parent directory where pdf_quality_report.py is located[m
[31m-        sys.path.append(LMS_DATABASE_DIR)[m
[32m+[m[32m        sys.path.append(os.path.join(os.path.dirname(__file__), '..'))[m
         from pdf_quality_report import reset_all_data[m
         [m
         # Reset everything[m
         results = reset_all_data(app.config['PROCESSED_FOLDER'])[m
         [m
[31m-        # Reset session data - clear current session and start fresh for next upload batch[m
[31m-        session_reset_success = False[m
[31m-        if session_manager.current_session:[m
[31m-            session_reset_success = session_manager.reset_session()[m
[31m-            logger.info(f"📋 Session reset: {'✅ Success' if session_reset_success else '❌ Failed'}")[m
[31m-        else:[m
[31m-            logger.info("📋 No active session to reset")[m
[31m-        [m
[31m-        # Always start a fresh session for the next upload batch[m
[31m-        try:[m
[31m-            session_id = session_manager.start_new_session("web_user")[m
[31m-            session_reset_success = True[m
[31m-            logger.info(f"✅ Started fresh session for next uploads: {session_id}")[m
[31m-        except Exception as e:[m
[31m-            logger.error(f"❌ Failed to start new session: {e}")[m
[31m-            session_reset_success = False[m
[31m-        [m
         # Determine overall success[m
[31m-        success = results['database_cleared'] and len(results['errors']) == 0 and session_reset_success[m
[32m+[m[32m        success = results['database_cleared'] and len(results['errors']) == 0[m
         [m
         response_data = {[m
             'success': success,[m
[36m@@ -419,11 +430,7 @@[m [mdef reset_all_data_endpoint():[m
             'details': {[m
                 'database_cleared': results['database_cleared'],[m
                 'files_deleted': results['files_deleted'],[m
[31m-                'pdf_reports_deleted': results['pdf_reports_deleted'],[m
[31m-                'orphan_records_deleted': results.get('orphan_records_deleted', 0),[